author: "Will"
delete-after: True
changes:
  - code_imp: "Cleanup weakref references that were lingering after Destroy()"
  - bugfix: "resleeving pod no longer runtime the resleeving console forever if containers are inside it when it is qdeleted"