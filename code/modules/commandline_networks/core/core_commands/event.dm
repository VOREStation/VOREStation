/*
Wrapper for the event interface, namely:
	Clear - clears all events
	Remove - removes all events under the alias provided
	Add - adds an event (command) to the alias provided
*/
/datum/commandline_network_command/event
	name = "event"
